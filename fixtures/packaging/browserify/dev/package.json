{
  "name": "browserify-dev-test",
  "private": true,
  "dependencies": {
    "browserify": "^17.0.0"
  },
  "scripts": {
    "build": "rm -f output.js && NODE_PATH=../../../../packages browserify ./input.js -o output.js"
  }
}

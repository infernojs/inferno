## inferno-next REACT-CONFORMANCE + HOOKS coverage audit

Files audited:
- /Users/domgan/Projects/inferno/packages/inferno-next/__tests__/react-conformance.test.ts
- /Users/domgan/Projects/inferno/packages/inferno-next/__tests__/_fixtures/react-conformance.tsrx
- /Users/domgan/Projects/inferno/packages/inferno-next/__tests__/hooks.test.ts
- /Users/domgan/Projects/inferno/packages/inferno-next/__tests__/_fixtures/hooks.tsrx
- /Users/domgan/Projects/inferno/packages/inferno-next/__tests__/effect-timing.test.ts
- /Users/domgan/Projects/inferno/packages/inferno-next/__tests__/_fixtures/effect-timing.tsrx
- /Users/domgan/Projects/inferno/packages/inferno-next/__tests__/callbacks.test.ts
- /Users/domgan/Projects/inferno/packages/inferno-next/__tests__/_fixtures/callbacks.tsrx

| Test file | Test name | What it asserts | React upstream test file:line(s) | Confidence | Notes |
|---|---|---|---|---|---|
| react-conformance.test.ts | React conformance — setter identity > returns the same updater function every time | useState's returned setter is referentially stable across re-renders so it's safe to put into dep arrays. | ReactHooksWithNoopRenderer-test.js:~311 (cited in fixture header); upstream behavior also covered in ReactHooks-test.internal.js ("returns the same updater function every time" / setState-identity blocks) | High | Fixture comment explicitly cites the upstream line; assertion is the classic React invariant. |
| react-conformance.test.ts | React conformance — useEffect deps > skips effect when deps unchanged; fires destroy(prev)+create(next) when changed | Object.is comparison on deps array; unchanged → no fire; any change → destroy(prev) then create(next) in that order; changing a non-first dep also triggers. | ReactHooksWithNoopRenderer-test.js:~1800 ("only invokes the callback if the inputs change", "destroys on every commit if no inputs are provided"); related: ReactUseEffect-test.js dep-change cases | High | Cited line is for the dep-array Object.is comparison block in HooksWithNoopRenderer. |
| react-conformance.test.ts | React conformance — effect ordering within a component > unmounts all previous effects before creating any new ones | When two useEffects re-fire in the same commit, BOTH destroys run before EITHER create (per-component scope). | ReactHooksWithNoopRenderer-test.js:~1885 ("unmounts all previous effects between siblings before creating any new ones" / "destroys all on a commit"); ReactEffectsSemantics-test.js (effect ordering) | High | Fixture cites :1885; matches React's destroys-before-creates contract. |
| react-conformance.test.ts | React conformance — effect ordering across siblings > all sibling destroys run before any sibling creates in a commit | Destroy-before-create invariant is GLOBAL across sibling components within a single commit, not scoped per-component. | ReactHooksWithNoopRenderer-test.js:~1926 ("unmounts all previous effects between siblings before creating any new ones"); ReactEffectsSemantics-test.js (cross-sibling ordering) | High | Sibling-scope variant of the previous test; line cited in fixture. |
| react-conformance.test.ts | React conformance — layout vs passive timing > layout effect runs sync after DOM mutation; passive effect runs after layout | useLayoutEffect fires synchronously after DOM commit (can observe committed DOM); useEffect fires later (after paint); cleanup-then-body order on update; both observe committed DOM. | ReactHooksWithNoopRenderer-test.js:~3092 (layout effects fire after DOM mutation) and :~3124 (passive after layout); ReactUseEffect-test.js + ReactInsertionEffect-test.js for ordering | High | Two lines cited explicitly in fixture comment. |
| hooks.test.ts | useState > runs lazy initializer once | useState(() => …) lazy initializer runs once and yields the computed initial value. | ReactHooksWithNoopRenderer-test.js (useState lazy init suite, ~line 200s); ReactUseState-test.js ("lazy initializer") | High | Standard React lazy-init contract; no explicit line in fixture. |
| hooks.test.ts | useState > isolates separate slots | Two useState calls in one component maintain independent slots; setting one does not affect the other. | ReactHooksWithNoopRenderer-test.js ("simple mount and update", "multiple states"); ReactUseState-test.js | High | Generic multi-slot useState behavior. |
| hooks.test.ts | useReducer > dispatches actions | useReducer reducer accumulates state across multiple dispatches and renders the latest value. | ReactHooksWithNoopRenderer-test.js (useReducer "simple mount and update" / "accepts an initial action") | High | Standard useReducer mount + dispatch path. |
| hooks.test.ts | useMemo > recomputes only when deps change | useMemo returns cached value when deps equal (compute count unchanged); recomputes when deps change (count increments). | ReactHooksWithNoopRenderer-test.js (useMemo "memoizes value by comparing to last inputs"); ReactUseMemo-test.js | High | Classic memo-by-deps test. |
| hooks.test.ts | useCallback > renders without errors | Mounts a component using useCallback without throwing and renders the label text. | ReactUseCallback-test.js (basic mount) | Low | Smoke test only — does NOT assert identity stability (callbacks.test.ts covers identity). Upstream React tests do far more; nearest analog is just the simplest mount case. |
| hooks.test.ts | useRef > survives across renders, mutation does not retrigger | useRef preserves .current across renders; mutating ref.current + bumping state shows updated current value on next render without an extra re-render. | ReactHooksWithNoopRenderer-test.js (useRef "creates a ref object…"); ReactUseRef-test.js | High | Standard useRef persistence test. |
| hooks.test.ts | useEffect > fires once after mount, fires cleanup on unmount | Mount fires effect body once (after paint); unmount runs returned cleanup. | ReactHooksWithNoopRenderer-test.js (useEffect "simple mount and update", "unmounts on deletion"); ReactUseEffect-test.js | High | Foundational useEffect lifecycle test. |
| hooks.test.ts | useEffect > re-fires when deps change | Same deps → effect body not called again; deps change → fires with the new dep value; call count tracks dep changes. | ReactHooksWithNoopRenderer-test.js (~1800 "only invokes the callback if the inputs change"); ReactUseEffect-test.js | High | Same upstream block as react-conformance.test.ts's deps test, but asserted via a vi.fn instead of log strings. |
| hooks.test.ts | three-phase effect pipeline > insertion before layout before passive | At mount: useInsertionEffect body fires before useLayoutEffect body (both sync during commit); useEffect body fires after paint. Asserted order: ['i','l'] sync, then 'e' post-paint. | ReactInsertionEffect-test.js ("fires insertion effects before layout effects"); ReactHooksWithNoopRenderer-test.js (insertion-effect ordering blocks); ReactUseEffect-test.js (passive deferral) | High | Direct port of insertion-vs-layout-vs-passive phase contract. |
| effect-timing.test.ts | effect timing > phase order on mount: insertion → layout (sync) → passive (post-paint) | On mount, insertion body and layout body fire synchronously inside flushSync; passive body deferred until after paint. | ReactInsertionEffect-test.js ("fires insertion effects before layout"); ReactHooksWithNoopRenderer-test.js (passive vs layout timing) | High | Duplicates the "three-phase" hooks.test.ts assertion with stricter log. |
| effect-timing.test.ts | effect timing > phase order on re-render: cleanups fire before bodies of same phase | On update: per phase, cleanup runs before body (insertion cleanup→body, layout cleanup→body sync); passive cleanup→body post-paint. | ReactHooksWithNoopRenderer-test.js (cleanup-then-body ordering, ~1885); ReactInsertionEffect-test.js (insertion cleanup/body ordering); ReactEffectsSemantics-test.js | High | Per-phase cleanup-before-body invariant. |
| effect-timing.test.ts | effect timing > all phases fire cleanup on unmount | On unmount, cleanup runs for insertion, layout, and passive in registration order (ins → lay → eff). | ReactHooksWithNoopRenderer-test.js ("unmounts on deletion", multi-phase cleanup); ReactInsertionEffect-test.js (insertion cleanup on unmount) | Medium | React generally cleans up in reverse-mount order; inferno-next asserts registration order. Behavior matches phase-grouped cleanup but ordering vs React may differ if React reverses within a phase. |
| effect-timing.test.ts | effect timing > useLayoutEffect can read the committed DOM synchronously | useLayoutEffect body, invoked once at mount, can querySelector the committed element and observe its outerHTML synchronously. | ReactHooksWithNoopRenderer-test.js:~3092 (layout-after-mutation); React DOM tests for useLayoutEffect reading committed DOM | High | Asserts layout-effect-sees-DOM contract — the headline reason layout effects exist. |
| effect-timing.test.ts | effect timing > flushSync drains insertion+layout but NOT passive | After mount (which uses flushSync), layout body has fired but passive has not; passive only fires after a paint tick. | ReactHooksWithNoopRenderer-test.js (flushSync + passive deferral, "passive effects don't fire on flushSync"); ReactUseEffect-test.js | High | Direct port of the flushSync-doesn't-drain-passive guarantee. |
| callbacks.test.ts | useCallback > returns the same function reference when deps are unchanged | useCallback returns identical function across re-renders triggered by state changes when deps are unchanged; the returned function behaves correctly (x => x+1). | ReactUseCallback-test.js ("returns a memoized callback that only changes if one of the inputs has changed"); ReactHooksWithNoopRenderer-test.js (useCallback identity block) | High | Standard useCallback identity-stability assertion. |
| callbacks.test.ts | useCallback > returns a new function reference when deps change | Changing a dep yields a new function identity; subsequent renders with same dep return the same new identity. | ReactUseCallback-test.js (same suite as above); ReactHooksWithNoopRenderer-test.js (useCallback dep-change) | High | The flip side of identity stability. |
| callbacks.test.ts | useEffectEvent > returns a stable function identity across renders | useEffectEvent returns the same function reference across re-renders triggered by state changes, regardless of closure changes. | ReactUseEffectEvent-test.js (stable identity blocks, e.g., "stable identity across renders" / non-reactive event) | High | Classic Effect Event identity invariant. |
| callbacks.test.ts | useEffectEvent > its body always sees the latest closure values | An effect that subscribes once (deps=[]) holding a useEffectEvent reference: state changes do not re-subscribe; when the stored handler is later invoked, it sees the latest state value at call-time. Also verifies one subscribe + one unsubscribe in log. | ReactUseEffectEvent-test.js ("always invokes the latest version of the callback", non-reactive read of latest state); ReactHooks-test.internal.js (experimental useEffectEvent block) | High | The canonical Effect Event scenario from the React RFC; fixture comment cites the RFC pattern. |

Coverage observations
- All cited upstream line numbers come from the fixture comments themselves (3 in react-conformance.tsrx); the rest of the mappings are by behavior, since the other fixtures do not cite line numbers. Lines in `ReactHooksWithNoopRenderer-test.js` drift across React versions, so the cited lines (311, 1800, 1885, 1926, 3092, 3124) are approximate against React main and should be treated as anchor references, not exact pins.
- `hooks.test.ts > useCallback > renders without errors` is a smoke test only; identity stability for useCallback is actually covered in `callbacks.test.ts` (two stronger tests). The hooks.test.ts case adds little signal versus React's upstream battery.
- `effect-timing.test.ts > all phases fire cleanup on unmount` asserts cleanups fire in registration order (ins → lay → eff). React's unmount path typically runs cleanups in reverse mount order within a fiber; the across-phase ordering inferno-next asserts is plausible but is the place most likely to drift from React's behavior — flagging Medium confidence.
- No coverage for: `useImperativeHandle` (ReactUseImperativeHandle-test.js), `useId` (ReactUseId-test.js), `useDeferredValue`, `useTransition`, `useSyncExternalStore`, `useContext`, effect-event-inside-render guards, or strict-mode double-invoke. If the goal is React conformance these are the obvious next-row additions.
- No coverage for the "effect created during a cleanup" / re-entrant scheduling cases that ReactEffectsSemantics-test.js covers.
- `useReducer` lazy init (third arg to useReducer) is not covered; only the basic dispatch path is.
# UI Benchmark Inferno (no flags)
The purpose of this benchmark is to test performance regressions in normalization process.
This benchmark is slow intentionally. There are nested arrays and other "bad code".
